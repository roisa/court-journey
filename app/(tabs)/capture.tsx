/**
 * Placeholder for the center "Capture" tab. The tab never actually navigates
 * here — its tabPress is intercepted in (tabs)/_layout.tsx to open the
 * /capture modal instead. This file exists only to register the tab slot.
 */
export default function CaptureTabPlaceholder() {
  return null;
}
