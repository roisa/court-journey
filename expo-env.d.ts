/// <reference types="expo/types" />

// NOTE: This file should not be edited and should be in your .gitignore in a
// normal Expo project, but we keep it tracked so `tsc` works without running
// `expo start` first in this headless environment.
